# Corpus triage — audio domain, 2026-07-02

195 keyword candidates → 54 kept (see `corpus.audio.json`, each with a `why`).
Drop reasons, by class — veto any of this by re-adding the URL to the corpus:

- **False positives (keyword matched the wrong thing):** `swift-test-codecov` ×2
  ("codec"), `ShortcutRecorder` ("recorder"), `stendahls/Taskig` ("hls" inside the
  owner name), `apple/swift-profile-recorder`, `CodeCallTracker` ("codec"),
  `safetysynth` ("synth" = reactive synthesis), `arachne` ("synth" in "absynthium"),
  `spring-media/Carlos` (caching), `bazaarvoice` ("voice"), `teufelaudio/*` (owner
  is an audio company, packages aren't audio), `soundcloud/Axt` (testing lib),
  `SwiftSpatial`/`SpatialExtensions`/`OpenSpatial` (visionOS math, not spatial audio),
  `MediaType`/`HTTPMediaTypes`/`swift-multicodec` (MIME/encoding), misc "player"
  owner names.
- **Service-API clients, not runtime capability:** audd-swift, FreesoundKit,
  PodcastIndexKit, audiobookshelf-api, openai-videos, gemini-omni-video,
  speechall-sdk, w3w-voice-api, SoundCloud-Swift, HCVimeoVideoExtractor.
- **Commercial binary SDKs (v1 extraction can't see into them; revisit with
  xcframework Info.plist extraction):** Twilio, Agora ×6, Vonage/OpenTok, Zego ×2,
  webrtc-sdk/Specs, flowplayer, Google IMA ×2, signicat. `stasel/WebRTC` stays
  seeded as the representative binary case.
- **Music theory / notation (a future domain, not audio runtime):** Tonic,
  musictheory, dn-m/Music + MusicXML, notus, swift-music ×2, Microtonality,
  PianoRoll, Keyboard, Flow, Controls, KissFFT.
- **Small/duplicative utilities (revisit on demand):** WW* family, MLAudioPlayer,
  MLVideoPlayer, SVEVideoUI, MediaViewer, PBMediaSlider, swiftui background/loop
  video players, SoundManager/SpeechManager, SwiftSound, SoundKit,
  SoundPulseButton, Audiograph, media-utilities, AudioVisualService,
  core-video-tools, VideoHash, DPVideoMerger, TranscodeVideo, LiveAudioServer,
  AudioProcessor, dioKaratzas/swift-audio-kit, eelcokoelewijn/audiokit (dup),
  media-generation-kit, sbooth CXX*/utility layers + opus-binary-xcframework,
  orchetect swift-midi-{core,sync,ui,controlsurfaces} (io/file/umbrella kept),
  mlx-audio (Python; the -swift port is kept), gaelic-ghost forks, miniaudio,
  kotlin/rust ports, chris-swift-dev/Media, iankoex/*, Octopus*, octopuskit,
  swift-webgpu, USBDeviceSwift, PillboxView, Taskig, SwiftRetrier,
  media-size-watchdog, mux-stats (analytics), MyLibrary, SafeMediaKit,
  podcast-feed-maker ×2, swift-audio-marker, delta-codec, Edge-TTS (brewus;
  herrkaefer kept), TTSKit/tts/TextForSpeech/speech-swift/SpeechKit (obscure
  TTS dupes; five TTS packages already kept), speechmarkdown-rust,
  1amageek/alexpiezo WebRTC wrappers, Extension-Synervoz/Voicemod (Agora
  extensions), VideoUIKit ×2, FacelessVideosKit, swiftplayer, arachne.
